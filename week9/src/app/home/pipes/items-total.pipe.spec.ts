import { ItemsTotalPipe } from './items-total.pipe';

describe('ItemsTotalPipe', () => {
  it('create an instance', () => {
    const pipe = new ItemsTotalPipe();
    expect(pipe).toBeTruthy();
  });
});
